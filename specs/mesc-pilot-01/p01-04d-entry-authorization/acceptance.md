# P01-04D Entry Authorization — Acceptance

These criteria govern this documentation gate only.

```text
A PASSING ENTRY-AUTHORIZATION DOCUMENTATION GATE IS NEVER EXECUTION AUTHORITY.
```

Satisfying every criterion below records an entry decision. It opens no
protected input, creates no generation workspace, produces no artifact and
authorizes no execution.

## 1. Criteria

### 1. Exact canonical baseline

The package is built on exactly `9229fea8c208021d3bbdb3767e71c3e3f790262e`,
tree `14ee0ffa80573b19c70948dd9db3b282ec5634af`.

### 2. Exact eight-path documentation scope

Exactly eight changed paths and no ninth: four new files under
`specs/mesc-pilot-01/p01-04d-entry-authorization/` and four modified files
(`p01-04/artifact-contracts.md`, `p01-04/execution-protocol.md`,
`p01-04/plan.md`, `tasks.md`). No source, test, script, workflow, dependency,
lockfile, scientific-policy or artifact-schema change is included.

### 3. Exact founder entry decision

The decision is recorded exactly as `P01-04D ENTRY AUTHORIZED`, dated
`2026-08-05`, class `ENTRY / PRE-EXECUTION GOVERNANCE ONLY`.

### 4. Original verdict preserved historically

The original `NOT READY` verdict is preserved and framed as historical. It is
not rewritten or deleted.

### 5. B-1 closed at implementation level

The controlled operator invocation path exists in canonical code, with exactly
two commands, one generation identity per invocation, fully explicit inputs, and
complete validation before any mutation.

### 6. B-2 closed at implementation level

The exact seven-file candidate inventory is enforced by the implementation, with
no eighth artifact and no standalone fingerprint file, and the supersession map
and stage separation are honoured.

### 7. F1/F2/F3 closed

Independent workspace verification, the second repository-identity verification
and the exact manifest-contract validation are all closed and covered by tests.

### 8. Formal implementation canonically adopted

The formal-executor implementation code is canonically adopted on canonical
main.

### 9. Adoption truth canonically reconciled

The canonical status text matches the canonical code.

### 10. Entry-readiness re-evaluation READY

The independent re-evaluation returned
`READY FOR FOUNDER P01-04D ENTRY DISPOSITION` with zero new blocking findings.

### 11. Entry authorized

`P01-04D entry: AUTHORIZED` and
`P01-04D control state: ENTERED — PRE-EXECUTION GOVERNANCE ONLY`.

### 12. Execution not authorized

`P01-04D execution: NOT AUTHORIZED` appears and remains in force in every
document of this package and in the reconciled current state.

### 13. No protected-data access

No P01-03G registry content, no external source-record file and no real dataset
was opened, read, hashed, stat-ed, parsed or copied while producing this
package.

### 14. No Generation A or B

No generation workspace was created, no generation was performed, and neither
`generate` nor `compare` was invoked.

### 15. N-1 conservative stale wording corrected

The stale implementation-status wording in `p01-04/artifact-contracts.md` and
`p01-04/execution-protocol.md` is framed as historical at the remediation-design
baseline, with current truth recorded alongside it. No artifact name, schema,
descriptor, fingerprint rule, stage ownership, invocation parameter, validation
order or execution semantic is altered.

### 16. D1 through D10 unchanged

No ratified scientific decision is altered or reopened. Partitions, targets,
total, decision order, grouping key, holdout policy, accepted algorithm version
and accepted split seed are unchanged.

### 17. Exactly one live current-state owner

`P01-T03D3` is the sole live current-state owner. `P01-T03D2` and every earlier
block are historical or superseded, and their fields are preserved unrewritten.

### 18. Cross-document consistency

The canonical baseline, re-evaluation verdict, founder decision date, entry
status, control state, execution status, Generation A and Generation B status,
`P01-04 overall` and `P01-05` agree across all eight paths.

### 19. Zero prohibited affirmative execution claims

No added line asserts that execution, registry access, dataset access,
Generation A, Generation B, partition membership, leakage completion, evidence
publication, model execution, training or fine-tuning has been authorized or has
occurred, and no added line asserts that P01-04 is finished or that P01-05 is
open.

### 20. Exact three-commit candidate history and no publication

The entry-authorization candidate passes this criterion only when its history is
one linear three-commit chain above the exact canonical baseline:

```text
commit 1 subject:
docs(mesc): authorize P01-04D entry

commit 2 subject:
fix(mesc): correct P01-04D entry disposition narrative

commit 3 subject:
fix(mesc): reconcile entry-authorization acceptance truth

canonical parent of commit 1:
9229fea8c208021d3bbdb3767e71c3e3f790262e

parent count for every candidate commit:
1

commits above canonical main:
3

cumulative changed paths:
8

amends:
0

rebases:
0

squashes:
0

force-pushes:
0

pushes at this local acceptance gate:
0

remote branch at this local acceptance gate:
absent

pull-request mutations at this local acceptance gate:
0
```

The three commits must remain in the order shown above. The first records the
founder entry decision, the second closes independent-review finding F-1, and
the third reconciles the acceptance criterion and exact verdict wording with
the corrected candidate history.
The cumulative candidate must still contain exactly the authorized eight
documentation paths, with no ninth path and no protected-path change.
This criterion records the local build-and-review topology and publication
boundary at the time of this acceptance gate. A later separately authorized
push, pull request or canonical merge does not retroactively falsify this
historical gate record and does not authorize P01-04D execution.
Do not add any candidate commit SHA to repository content.
Do not mention a future PR number.

## 2. Non-authority

A pass on these criteria records the founder entry decision and nothing more. It
grants no execution authority, no input access and no later-stage authority. A
separate founder execution authorization is required before any protected input
may be opened or any generation command may run.
