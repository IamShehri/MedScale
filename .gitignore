# Local tooling / machine-specific settings (never part of the canonical foundation)
.claude/settings.local.json

# Python bytecode / build
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Tool caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# NOTE: uv.lock IS committed (reproducibility) — do not ignore it.

# Docs site build (MkDocs, if/when adopted)
site/

# OS / editor noise
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*~
.idea/
.vscode/

# Research raw data and acquisition artifacts
# Raw external datasets must remain outside the repository. These rules are
# defense-in-depth against accidental repository-local acquisition.
.medscale-data/
data/raw/
artifacts/raw/
research-data/raw/
