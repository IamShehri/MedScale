# Local tooling / machine-specific settings (never part of the canonical foundation)
.claude/settings.local.json

# Python bytecode / build
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Tool caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# NOTE: uv.lock IS committed (reproducibility) — do not ignore it.

# Docs site build (MkDocs, if/when adopted)
site/

# OS / editor noise
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*~
.idea/
.vscode/
